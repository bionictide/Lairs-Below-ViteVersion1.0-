// Client-side rendering only
export class LootUIManager {
  constructor(scene) {
    this.scene = scene;
  }

  displayBagContents(bag) {
    console.log("[LootUIManager] Display bag:", bag);
    // TODO: implement visual loot UI rendering
  }
}
